apiVersion: v1
kind: Service
metadata:
  labels:
    app: payment-service
  name: payment-service
  namespace: fiap
spec:
  type: NodePort
  ports:
  - name: 3000-8181
    port: 3000
    targetPort: 8181
    nodePort: 30009
  selector:
    app: payment
