apiVersion: v1
kind: Service
metadata:
  labels:
    app: alb-lb
  name: alb-lb
  namespace: fiap
spec:
  type: NodePort
  ports:
  - name: 3000-8080
    port: 3000
    targetPort: 8080
    nodePort: 30007
  selector:
    app: payment
